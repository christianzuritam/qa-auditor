# QA potenciado con IA

Web para auditar una orden de Happy Fox versus la configuración en plataforma (Meta, TikTok, Google) usando IA.

## Requisitos

- Node.js 18+
- API key de Claude (Anthropic)

## Ejecutar

1. Instala dependencias:
   ```bash
   npm install
   ```
2. Crea `.env` a partir de `.env.example` y define `ANTHROPIC_API_KEY`.
3. Inicia el servidor:
   ```bash
   npm start
   ```
4. Abre `http://localhost:3000`.
